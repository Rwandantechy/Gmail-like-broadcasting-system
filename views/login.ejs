<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Nishkaam Innovations | Login</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="styles/tailwind.css" />
  </head>

  <body
    class="bg-gradient-to-b from-green-200 to-transparent flex items-center justify-center min-h-screen"
  >
    <div
      class="w-full max-w-md px-4 py-8 bg-gradient-to-b from-green-200 via-yellow-100 to-purple-100 rounded-lg shadow-md sm:px-6 md:px-8 lg:px-10"
    >
      <div
        class="self-center mb-6 text-2xl text-center font-semibold text-meta-4"
      >
        Welcome to Nishkaam Innovations!
      </div>
      <form action="/auth/login" method="POST" class="flex flex-col gap-4">
        <!-- Email Address -->
        <div>
          <label
            for="LoggingEmailAddress"
            class="block mb-1 text-sm font-medium text-gray-600"
            >Email Address</label
          >
          <input
            id="LoggingEmailAddress"
            type="text"
            name="email"
            placeholder="Email Address"
            class="w-full px-4 py-2 text-sm leading-tight text-gray-700 border rounded focus:outline-none focus:shadow-outline"
          />
        </div>
        <!-- Password -->
        <div class="flex justify-between items-center">
          <label
            for="LoggingPassword"
            class="block mb-1 text-sm font-medium text-gray-600"
            >Password</label
          >
        </div>

        <input
          id="LoggingPassword"
          type="password"
          name="password"
          placeholder="Password"
          class="w-full px-4 py-2 mb-2 text-sm leading-tight text-gray-700 border rounded focus:outline-none focus:shadow-outline"
        />
        <div class="flex items-start">
          <div class="flex items-start">
            <div class="flex items-center h-5">
              <input
                id="remember"
                type="checkbox"
                value=""
                class="w-6 h-6 border border-gray-300 rounded bg-gray-50 focus:ring-3 focus:ring-blue-300 dark:bg-gray-700 dark:border-gray-600 dark:focus:ring-blue-600 dark:ring-offset-gray-800 dark:focus:ring-offset-gray-800"
                required
              />
            </div>
            <label
              for="remember"
              class="ms-4 pb-5 text-lg font-medium text-gray-900 dark:text-gray-300"
            >
              &nbsp; Remember me</label
            >
          </div>
          <a
            href="forgot-password"
            class="ms-auto text-lg ml-5 text-blue-700 hover:underline dark:text-blue-500"
            >Lost Password?</a
          >
        </div>
        <button
          type="submit"
          class="w-full bg-green-600 px-4 py-2 text-black transition-colors duration-200 transform  rounded hover:bg-purple-500 focus:outline-none focus:bg-green-500"
        >
          LOGIN
        </button>

        <div
          class="text-lg ml-2 pb-3 font-medium text-gray-500 dark:text-gray-300"
        >
          Not registered?
          <a
            href="signup"
            class="text-blue-700 hover:underline dark:text-blue-500"
            >Create account</a
          >
        </div>
      </form>
    </div>
  </body>
</html>
